import { HairColorPipe } from './hair-color.pipe';

describe('HairColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HairColorPipe();
    expect(pipe).toBeTruthy();
  });
});
